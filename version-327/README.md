# 欧美高清影院 静态电影网站

本包由上传素材生成，包含 2000 部影片数据、2000 个详情页、10 个独立分类页、首页、分类总览页、排行榜页、搜索筛选页、站点 CSS/JS、sitemap 和 robots。

图片引用规则：所有封面、Hero、卡片背景均按顶级目录 `1.jpg` 到 `150.jpg` 循环引用。原压缩包中未包含这些 JPG 文件；部署时可将图片放入站点根目录，页面会自动显示。

播放器：详情页播放器绑定了原 JS 中提取的 m3u8 播放源，并包含 HLS 初始化逻辑；现代浏览器可通过 Hls.js 或原生 HLS 播放。
